export default {
  id: "god",
  tag: "integration",
  icon: "👑",
  title: "神レベル",
  short: "システム全体を統べる設計・運用の極意",
  description:
    "言語を超えて『良いシステムとは何か』を学ぶ最終章。設計原則、システム設計、セキュリティ、パフォーマンス、テストと運用、そして総合演習。ここを越えれば、あなたは開発の神。",
  note: "神レベルは特定言語ではなく『設計・運用の思考法』が中心です。これまでのJava/JavaScript/SQLの知識を総動員して読み解きましょう。",
  lessons: [
    {
      id: "god-1",
      title: "設計原則 ― SOLIDとクリーンな構造",
      level: 4,
      duration: "16分",
      body: `
# 設計原則 ― SOLIDとクリーンな構造

動くコードは誰でも書けます。神が書くのは**変更に強いコード**です。その指針が設計原則です。

## SOLID原則

- **S** 単一責任の原則 … 1つのクラス/関数は1つの理由でだけ変更される
- **O** 開放閉鎖の原則 … 拡張に開き、修正に閉じる（既存を壊さず機能追加）
- **L** リスコフの置換原則 … 子クラスは親の代わりに使える
- **I** インターフェース分離の原則 … 使わないメソッドに依存させない
- **D** 依存性逆転の原則 … 具体ではなく抽象に依存する

## 単一責任の例

\`\`\`java
// ❌ 1クラスが「計算」も「保存」も「通知」も持つ
class Order {
  void calcTotal() {}
  void saveToDB() {}
  void sendEmail() {}
}

// ✅ 責任ごとに分割
class Order { int calcTotal() { ... } }
class OrderRepository { void save(Order o) {} }
class MailService { void notify(Order o) {} }
\`\`\`

## 依存性逆転（DI）の心

上位（業務ロジック）が下位（DBやメール）の**具体実装に直接依存しない**。間に「インターフェース」を挟みます。

> こうすると、DBをMySQLからPostgreSQLに替えても業務ロジックは無傷。テスト時は偽のDBに差し替えられる。

## DRY / KISS / YAGNI

- **DRY**: 同じことを繰り返さない
- **KISS**: シンプルに保て
- **YAGNI**: 今要らない機能は作るな

:::tip
原則は「ルール」ではなく「判断の物差し」。盲信せず、なぜその原則があるのか（変更コストを下げるため）を理解して使い分けるのが神の所業です。
:::
`,
      quiz: [
        {
          q: "SOLIDの「S」、1つのクラスは1つの理由でだけ変更されるべき、という原則は？",
          choices: ["単一責任の原則", "開放閉鎖の原則", "依存性逆転の原則"],
          answer: 0,
          explain: "単一責任の原則(SRP)です。責任が混ざると変更が波及しやすくなります。",
        },
        {
          q: "「上位モジュールは具体実装ではなく抽象に依存すべき」という原則は？",
          choices: ["リスコフの置換原則", "依存性逆転の原則", "インターフェース分離の原則"],
          answer: 1,
          explain: "依存性逆転の原則(DIP)。間にインターフェースを挟み、差し替え可能にします。",
        },
        {
          q: "「今は必要ない機能を先回りして作るな」を表す言葉は？",
          choices: ["DRY", "KISS", "YAGNI"],
          answer: 2,
          explain: "YAGNI (You Aren't Gonna Need It)。過剰な作り込みを戒めます。",
        },
      ],
    },
    {
      id: "god-2",
      title: "システム設計 ― スケールする仕組み",
      level: 4,
      duration: "17分",
      body: `
# システム設計 ― スケールする仕組み

ユーザーが10人から1000万人になっても落ちないシステムは、どう設計されているのか。

## 3層 → 分散へ

\`\`\`
[ブラウザ] → [ロードバランサ] → [Webサーバー×N] → [キャッシュ] → [DB(主/従)]
\`\`\`

## スケールの2方向

- **垂直スケール**: サーバーを強くする（CPU/メモリ増強）。簡単だが限界あり
- **水平スケール**: サーバーを増やす（台数を足す）。ほぼ無限だが「状態をどこに置くか」が課題

水平スケールのため、Webサーバーは**ステートレス**（状態を持たない）に作るのが鉄則。セッションはRedis等の共有ストアへ。

## キャッシュ

何度も同じ計算/取得をしないために結果を保存。

- ブラウザキャッシュ / CDN（静的ファイル）
- アプリキャッシュ（Redis/Memcached）
- DBクエリ結果のキャッシュ

> 「コンピュータサイエンスの難問は2つ。キャッシュの無効化と、命名」と言われるほど、キャッシュは強力で難しい。

## 非同期・キュー

重い処理（メール送信、画像変換）は**メッセージキュー**に積んで後で処理。レスポンスを待たせない。

\`\`\`
注文API → キューに入れる → 即「受付完了」を返す
            ↓（別ワーカーが）
        メール送信・在庫更新
\`\`\`

## CAP定理（分散の宿命）

分散システムは「一貫性・可用性・分断耐性」を同時に完全達成できない。**何を諦めるか**を選ぶのが設計。

:::tip
最初から巨大設計をするのは悪手（YAGNI）。まずシンプルに作り、ボトルネックを**計測して**から対処する。神は推測せず計測する。
:::
`,
      quiz: [
        {
          q: "サーバーの台数を増やして処理能力を上げる方法は？",
          choices: ["垂直スケール", "水平スケール", "ダウンスケール"],
          answer: 1,
          explain: "水平スケール（スケールアウト）です。垂直は1台を強化します。",
        },
        {
          q: "水平スケールのためWebサーバーをどう作るべき？",
          choices: ["各サーバーに状態を持たせる", "ステートレスにし状態は共有ストアへ", "1台に集約する"],
          answer: 1,
          explain: "ステートレスにし、セッション等は共有ストア(Redis等)に置くのが定石です。",
        },
        {
          q: "重い処理を後回しにしてレスポンスを速く返す仕組みは？",
          choices: ["メッセージキュー", "インデックス", "正規化"],
          answer: 0,
          explain: "メッセージキューに積み、別ワーカーが非同期で処理します。",
        },
      ],
    },
    {
      id: "god-3",
      title: "セキュリティの極意",
      level: 4,
      duration: "16分",
      body: `
# セキュリティの極意

一箇所の綻びが全てを台無しにします。神は「攻撃者の視点」で自分のシステムを見ます。

## 認証 と 認可

- **認証(Authentication)**: あなたは誰か？（ログイン）
- **認可(Authorization)**: あなたは何をして良いか？（権限）

混同は重大事故のもと。「ログインできる＝何でもできる」ではない。

## 代表的な脆弱性（OWASP）

- **SQLインジェクション**: 入力をSQLに連結しない → プレースホルダを使う
- **XSS（クロスサイトスクリプティング）**: 出力をエスケープし、ユーザー入力をHTMLにそのまま埋め込まない
- **CSRF**: 偽のリクエスト対策にトークンを使う
- **認可不備**: URLを直接叩かれても他人のデータを見せない（サーバーで毎回チェック）

## パスワードと秘密情報

- パスワードは**ソルト付きハッシュ**(bcrypt/Argon2)で保存。暗号化や平文は不可
- APIキーや接続情報は**コードに書かない**。環境変数や秘密管理サービスへ
- 通信は必ず **HTTPS**

## 多層防御と最小権限

- 1つの対策に頼らず層で守る（入力検証＋エスケープ＋WAF＋監視）
- 各コンポーネントには**必要最小限の権限**だけ与える

:::warn
「フロントで検証したから安全」は誤り。フロントの検証はUX向上のためで、**セキュリティ上の検証は必ずサーバー側でも行う**こと。攻撃者はフロントを通りません。
:::
`,
      exercises: [
        {
          type: "sql",
          label: "▶ 攻撃を体感する（SQLインジェクションの考え方）",
          spec: {
            starter: `-- もし入力 name を文字列連結していたら…
-- "' OR '1'='1" を入れると WHERE が常に真になり全件漏洩する。
-- 下は「常に真」になってしまう危険なクエリの例:
SELECT * FROM users WHERE name = '' OR '1'='1';
-- 対策はプレースホルダ(?)でSQLと値を分離すること。`,
          },
        },
      ],
      quiz: [
        {
          q: "「あなたは何をして良いか（権限）」を確認することを何という？",
          choices: ["認証(Authentication)", "認可(Authorization)", "暗号化"],
          answer: 1,
          explain: "認可(Authorization)です。認証は『誰か』、認可は『何をして良いか』。",
        },
        {
          q: "パスワードの保存方法として正しいのは？",
          choices: ["平文で保存", "ソルト付きハッシュ(bcrypt等)で保存", "URLに付けて保存"],
          answer: 1,
          explain: "ソルト付きハッシュで保存します。平文・可逆暗号は不可です。",
        },
        {
          q: "ユーザー入力をHTMLにそのまま出力すると起きうる攻撃は？",
          choices: ["XSS", "正規化", "キャッシュミス"],
          answer: 0,
          explain: "XSSです。出力時のエスケープで防ぎます。",
        },
      ],
    },
    {
      id: "god-4",
      title: "パフォーマンスチューニング",
      level: 4,
      duration: "15分",
      body: `
# パフォーマンスチューニング

速さは機能です。神は「推測するな、計測せよ」を徹底します。

## まず計測

- どこが遅いか**プロファイラ**で特定（当て推量で最適化しない）
- 「全体の95%の時間を食う5%のコード」を直す（パレートの法則）

## DBが最大のボトルネックになりがち

### N+1問題

一覧1回 + 各行ごとに1回ずつクエリ＝大量のクエリ。

\`\`\`
❌ ユーザー100人を取得 → 各人の注文を1件ずつ100回問い合わせ（合計101回）
✅ JOIN または IN でまとめて1〜2回に
\`\`\`

### その他

- 適切な**インデックス**（上級編で学んだ通り）
- 必要な列だけ取得（\`SELECT *\` を避ける）
- ページング（\`LIMIT\` / \`OFFSET\`）で一度に大量を返さない

## アプリ層の定石

- **キャッシュ**で重い計算/取得を再利用
- 重い処理は**非同期/キュー**へ
- フロントは画像最適化・遅延読み込み・バンドル削減

## 速さの指標

- レイテンシ（1回の応答時間）とスループット（単位時間の処理数）は別物
- 平均だけでなく**p95/p99**（遅い方の裾）を見る

:::tip
最適化は副作用（複雑化）を伴う。**ボトルネックでないコードの最適化は害**。計測 → 一点突破 → 再計測、を回しましょう。
:::
`,
      exercises: [
        {
          type: "sql",
          label: "▶ N+1 を1クエリに畳む",
          spec: {
            starter: `-- ❌ N+1: まず全ユーザー → その後ユーザーごとに注文を問い合わせる…を
-- ✅ 1クエリにまとめる（各ユーザーの注文件数を一括集計）
SELECT u.name, COUNT(o.id) AS 注文件数
FROM users u
LEFT JOIN orders o ON o.user_id = u.id
GROUP BY u.id
ORDER BY 注文件数 DESC;`,
          },
        },
      ],
      quiz: [
        {
          q: "一覧取得後に各行ごとに追加クエリを発行してしまう非効率な問題は？",
          choices: ["N+1問題", "デッドロック", "メモリリーク"],
          answer: 0,
          explain: "N+1問題です。JOINやINでまとめて解消します。",
        },
        {
          q: "最適化を始める前に最初にすべきことは？",
          choices: ["とりあえずキャッシュを入れる", "計測してボトルネックを特定する", "全コードを書き直す"],
          answer: 1,
          explain: "「推測するな、計測せよ」。プロファイルで遅い箇所を特定してから手を打ちます。",
        },
        {
          q: "応答時間のばらつきを評価するとき、平均に加えて見るべき指標は？",
          choices: ["p95/p99（遅い側の裾）", "ファイルサイズ", "行数"],
          answer: 0,
          explain: "平均だけでは隠れる遅延を p95/p99 で確認します。",
        },
      ],
    },
    {
      id: "god-5",
      title: "テスト戦略と CI/CD・運用",
      level: 4,
      duration: "16分",
      body: `
# テスト戦略と CI/CD・運用

神のシステムは「壊れない」のではなく「壊れてもすぐ気づき、すぐ直せる」。それを支えるのが自動化です。

## テストピラミッド

\`\`\`
        /\\   E2E（少数・遅い・本物に近い）
       /  \\  結合テスト
      /____\\ 単体テスト（多数・速い・土台）
\`\`\`

- **単体テスト**: 関数/クラス単位。速くたくさん（JS: Jest、Java: JUnit）
- **結合テスト**: 複数部品の連携（DB込みなど）
- **E2Eテスト**: 画面から通しで（Playwright/Selenium）

## 良いテストの例（考え方）

\`\`\`javascript
// 入力 → 期待出力 を宣言的に確認
test("30歳以上を抽出する", () => {
  const r = adults([{name:"A",age:20},{name:"B",age:40}]);
  expect(r).toEqual(["B"]);
});
\`\`\`

> このプラットフォームの演習が「複数入力で関数をテスト」していたのも、まさにこの考え方です。

## CI/CD

- **CI（継続的インテグレーション）**: push のたびに自動でテスト/ビルド。壊れたらすぐ通知
- **CD（継続的デリバリ/デプロイ）**: 通ったら自動で本番へ
- このサイト自身も **GitHub Actions** で push → 自動デプロイしています

## 運用（本番で戦う）

- **ログ**: 何が起きたか記録（構造化ログ）
- **監視/メトリクス**: CPU・エラー率・レイテンシを可視化（Prometheus/Grafana等）
- **アラート**: 異常を自動通知
- **ロールバック**: まずい変更を即座に戻せるように

:::tip
「テストが無いコードはレガシーコード」。テストは未来の自分への保険であり、安心して変更するための翼です。
:::
`,
      quiz: [
        {
          q: "テストピラミッドで最も数を多くすべきなのは？",
          choices: ["E2Eテスト", "単体テスト", "手動テスト"],
          answer: 1,
          explain: "速くて安定な単体テストを土台に多く、E2Eは少数にします。",
        },
        {
          q: "push のたびに自動でテスト・ビルドを行う仕組みは？",
          choices: ["CI（継続的インテグレーション）", "正規化", "キャッシュ"],
          answer: 0,
          explain: "CIです。壊れた変更を早期に検知できます。",
        },
        {
          q: "本番で異常にいち早く気づくために必要なのは？",
          choices: ["ログ・監視・アラート", "より多くのコメント", "変数名を短くする"],
          answer: 0,
          explain: "ログ/監視/アラートで観測可能性(Observability)を確保します。",
        },
      ],
    },
    {
      id: "god-6",
      title: "総合演習 ― 神の一撃（ミニ設計）",
      level: 4,
      duration: "20分",
      body: `
# 総合演習 ― 神の一撃（ミニ設計）

最終課題です。これまでの初級〜神レベルの知識を総動員し、小さなシステムを**自分の頭で設計**してみましょう。

## お題：「読書記録アプリ」

ユーザーが読んだ本を記録し、一覧・検索・統計を見られるサービス。

### Step 1. データ設計（SQL）

最低限のテーブルを考えます。

\`\`\`sql
CREATE TABLE users (
  id INTEGER PRIMARY KEY,
  name TEXT NOT NULL
);
CREATE TABLE books (
  id INTEGER PRIMARY KEY,
  user_id INTEGER NOT NULL,   -- 外部キー → users.id
  title TEXT NOT NULL,
  author TEXT,
  rating INTEGER,             -- 1〜5
  read_at TEXT
);
\`\`\`

正規化（1つの事実は1か所）、外部キー、よく検索する列へのインデックスを意識します。

### Step 2. API設計（バックエンド/Java）

RESTで素直に。

\`\`\`
GET    /api/books         一覧（自分の本だけ＝認可）
POST   /api/books         登録
GET    /api/books/{id}    1件
PUT    /api/books/{id}    更新
DELETE /api/books/{id}    削除
GET    /api/stats         平均評価・冊数など（集計クエリ）
\`\`\`

Controller→Service→Repository の3層、入力検証、認可（他人の本を触らせない）を忘れずに。

### Step 3. フロント（JavaScript）

\`fetch\` で一覧取得 → DOMに描画。\`map\`/\`filter\` で並べ替えや検索。非同期とエラーハンドリング。

### Step 4. 品質と運用

- 単体テスト（評価の集計関数など）
- セキュリティ（プレースホルダ、認可、HTTPS）
- パフォーマンス（一覧で N+1 を避ける、インデックス）
- CI/CD で自動テスト＆デプロイ

## 仕上げの演習：統計クエリ

下のSQLエディタで、サンプルDBを使って「都市ごとの平均年齢」を集計してみましょう（設計→集計の感覚を最後にもう一度）。
`,
      exercises: [
        {
          type: "sql",
          label: "✏️ 最終課題: 都市ごとの平均年齢（高い順）",
          spec: {
            starter: `-- users を city ごとに、平均年齢 AVG(age) を求め、
-- 平均年齢の高い順に並べてください。列は city, AVG(age)。
`,
            expected: "SELECT city, AVG(age) FROM users GROUP BY city ORDER BY AVG(age) DESC;",
            requires: [
              { pattern: "group\\s+by", hint: "GROUP BY city でグループ化しましょう。" },
              { pattern: "avg", hint: "AVG(age) で平均年齢を求めましょう。" },
              { pattern: "order\\s+by", hint: "ORDER BY ... DESC で高い順に並べましょう。" },
            ],
          },
        },
      ],
      quiz: [
        {
          q: "テーブル設計で他テーブルを参照する列を何という？",
          choices: ["主キー", "外部キー", "インデックス"],
          answer: 1,
          explain: "外部キー(FK)です。books.user_id → users.id のように関連を表します。",
        },
        {
          q: "「他人の本を触らせない」ために各リクエストで必ず行うのは？",
          choices: ["認可チェック（サーバー側）", "フロントの色を変える", "ログイン画面の装飾"],
          answer: 0,
          explain: "サーバー側で毎回、その操作が許されるか認可チェックします。",
        },
        {
          q: "この最終章で繰り返された、神の最も大切な姿勢は？",
          choices: ["推測で最適化する", "計測し、学び続け、壊れても直せるようにする", "一度で完璧に作る"],
          answer: 1,
          explain: "計測・継続学習・回復力。神とは学び続けることをやめない人のことです。",
        },
      ],
    },
  ],
};

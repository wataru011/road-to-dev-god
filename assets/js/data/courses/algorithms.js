export default {
  id: "algorithms",
  tag: "algo",
  icon: "🧮",
  title: "アルゴリズムとデータ構造",
  short: "速く正しく解くための土台（発展）",
  description:
    "効率的なプログラムの土台。計算量(Big-O)の考え方、基本的なデータ構造、探索と整列を学びます。業務システムでも「なぜ遅いのか」を理解し、適切に直す力につながります。",
  note: "発展トピックです。日々の開発に必須ではありませんが、性能問題の理解や設計判断の質を一段引き上げます。",
  lessons: [
    {
      id: "algo-1",
      title: "計算量（Big-O）の考え方",
      level: 3,
      duration: "14分",
      body: `
# 計算量（Big-O）の考え方

「データが増えたとき、処理時間がどう増えるか」を表すのが計算量(Big-O)です。性能を語る共通言語です。

## よくある計算量（速い順）

- **O(1)** 定数時間: 配列の添字アクセス、ハッシュの参照
- **O(log n)** 対数: 二分探索（毎回半分に絞る）
- **O(n)** 線形: 全件ループ
- **O(n log n)**: 効率的な整列（マージソート等）
- **O(n²)** 二乗: 二重ループ（要素同士の総当たり）

## 直感

\`\`\`
n=1,000,000 のとき…
O(log n) ≈ 20回
O(n)     ≈ 100万回
O(n²)    ≈ 1兆回（現実的に終わらない）
\`\`\`

## 実務での効き方

- 二重ループ O(n²) を、ハッシュ(Map)を使って O(n) にする
- 線形探索 O(n) を、インデックス/二分探索で O(log n) に
- SQLの **N+1問題** も計算量の話（呼び出し回数を減らす）

:::tip
細かい定数より「nに対してどう伸びるか」が本質。小さいデータなら O(n²) でも十分。**大きくなる場所だけ**計算量を気にするのが現実的です。
:::
`,
      quiz: [
        {
          q: "データ量nに対し、処理が「半分ずつ絞られる」探索の計算量は？",
          choices: ["O(log n)", "O(n²)", "O(1)"],
          answer: 0,
          explain: "二分探索は毎回半分に絞るため O(log n) です。",
        },
        {
          q: "二重ループで要素同士を総当たりする計算量は？",
          choices: ["O(n²)", "O(1)", "O(log n)"],
          answer: 0,
          explain: "ネストしたループは O(n²)。データが増えると急激に遅くなります。",
        },
      ],
    },
    {
      id: "algo-2",
      title: "基本データ構造",
      level: 3,
      duration: "15分",
      body: `
# 基本データ構造

データの「持ち方」を変えると、得意な操作が変わります。目的に合った構造を選ぶのが効率の鍵です。

## 配列（Array）

- 添字での参照は **O(1)** で高速
- 途中への挿入/削除は **O(n)**（ずらしが必要）

## 連結リスト（Linked List）

- 先頭/途中への挿入・削除が得意
- 添字アクセスは苦手（先頭からたどる）

## スタックとキュー

- **スタック**: 後入れ先出し(LIFO)。「元に戻す」履歴など
- **キュー**: 先入れ先出し(FIFO)。処理待ち行列、メッセージキュー

## ハッシュ（Map / 連想配列）

- キーで値を **O(1)** 前後で出し入れ
- 「IDから素早く引く」「重複チェック」に最適
- JSの \`Map\`/オブジェクト、Javaの \`HashMap\`

## 木（Tree）

- 階層構造（組織図、フォルダ、DOM）
- 探索木は検索 **O(log n)**。DBのインデックス(B木)もこの仲間

:::tip
「ID で頻繁に引く」ならハッシュ。「順番に処理」ならキュー。**操作の種類で構造を選ぶ**と、自然と速くなります。
:::
`,
      exercises: [
        {
          type: "js",
          label: "✏️ ハッシュ(Map)で重複を除く",
          spec: {
            starter: `// 配列から重複を取り除いた新しい配列を返す関数 unique(arr) を作ってください。
// Set（ハッシュ的構造）を使うと O(n) で書けます。順序は元の出現順を保つこと。
// 例: unique([1,2,2,3,1]) → [1,2,3]
function unique(arr) {
  // ここに書く
}`,
            tests: [
              { call: "unique([1,2,2,3,1])", expect: [1, 2, 3] },
              { call: 'unique(["a","b","a"])', expect: ["a", "b"] },
              { call: "unique([])", expect: [] },
            ],
            requires: [
              { pattern: "Set|reduce|indexOf|includes", hint: "Set などを使って重複を判定しましょう。" },
            ],
          },
        },
      ],
      quiz: [
        {
          q: "「IDから値を素早く引く」のに最適なデータ構造は？",
          choices: ["ハッシュ(Map)", "連結リスト", "スタック"],
          answer: 0,
          explain: "ハッシュ（Map/HashMap）はキー参照が O(1) 前後で高速です。",
        },
        {
          q: "先入れ先出し(FIFO)のデータ構造は？",
          choices: ["キュー", "スタック", "木"],
          answer: 0,
          explain: "キューはFIFO。スタックは後入れ先出し(LIFO)です。",
        },
      ],
    },
    {
      id: "algo-3",
      title: "【神】探索・整列と実務での選び方",
      level: 4,
      duration: "15分",
      body: `
# 【神】探索・整列と実務での選び方

代表的なアルゴリズムを知り、「自分で実装する」より「正しく選んで使う」判断力を養います。

## 探索

- **線形探索** O(n): 先頭から順に探す。未整列でもOK
- **二分探索** O(log n): **整列済み**の前提で半分ずつ絞る。高速

\`\`\`javascript
// 二分探索（昇順配列から target の位置を探す）
function binarySearch(sorted, target) {
  let lo = 0, hi = sorted.length - 1;
  while (lo <= hi) {
    const mid = (lo + hi) >> 1;
    if (sorted[mid] === target) return mid;
    if (sorted[mid] < target) lo = mid + 1; else hi = mid - 1;
  }
  return -1;
}
\`\`\`

## 整列（ソート）

- バブルソート O(n²): 学習用。実務では使わない
- マージ/クイックソート O(n log n): 実用的
- **実務では言語標準の sort を使う**（十分に最適化済み）

## 神の判断：作るより選ぶ

- 標準ライブラリ・DB・既存実装で足りるなら**自作しない**（バグの温床）
- ただし**中身の計算量は理解しておく**（なぜ遅いか説明できる）
- 「正しく動く」→「速い」の順。**まず計測**（早すぎる最適化を避ける）

:::tip
神とは全アルゴリズムを暗記する人ではなく、**問題に合った道具を選び、トレードオフを説明できる**人。引き出しを増やし、計測で裏を取る。これが上達の最終形です。
:::
`,
      exercises: [
        {
          type: "js",
          label: "✏️ 二分探索を実装する",
          spec: {
            starter: `// 昇順に並んだ配列 sorted から target の添字を返す関数 search(sorted, target)。
// 見つからなければ -1。二分探索で実装してください（複数ケースで検証されます）。
function search(sorted, target) {
  // ここに書く
}`,
            tests: [
              { call: "search([1,3,5,7,9], 7)", expect: 3 },
              { call: "search([1,3,5,7,9], 1)", expect: 0 },
              { call: "search([1,3,5,7,9], 9)", expect: 4 },
              { call: "search([1,3,5,7,9], 4)", expect: -1 },
              { call: "search([], 1)", expect: -1 },
            ],
            requires: [
              { pattern: "while|for", hint: "ループで探索範囲を絞り込みましょう。" },
            ],
          },
        },
      ],
      quiz: [
        {
          q: "二分探索を使うための前提条件は？",
          choices: ["配列が整列済みであること", "配列が空であること", "要素が文字列であること"],
          answer: 0,
          explain: "二分探索は整列済みが前提。未整列なら線形探索か、先に整列します。",
        },
        {
          q: "実務での整列(ソート)の基本方針は？",
          choices: ["言語標準のsortを使う", "毎回バブルソートを自作する", "ソートしない"],
          answer: 0,
          explain: "標準のsortは十分最適化済み。中身(計算量)は理解しつつ、自作は避けます。",
        },
      ],
    },
  ],
};
